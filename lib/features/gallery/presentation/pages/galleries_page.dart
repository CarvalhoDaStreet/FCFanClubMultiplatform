import 'package:flutter/material.dart';

class GalleriesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Galleries'),
      ),
      body: Center(
        child: Text('Galleries Page'),
      ),
    );
  }
}
