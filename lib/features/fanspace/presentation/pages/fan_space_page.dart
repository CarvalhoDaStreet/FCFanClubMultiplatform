import 'package:flutter/material.dart';

class FanSpacePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fan Space'),
      ),
      body: Center(
        child: Text('Fan Space Page'),
      ),
    );
  }
}
