import 'package:flutter/material.dart';

class FixturesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fixtures'),
      ),
      body: Center(
        child: Text('Fixtures Page'),
      ),
    );
  }
}
